export interface Dictionary {
  nav: {
    about: string
    experience: string
    projects: string
    resume: string
  }
  resume: {
    pdf: string
    download: string
    open: string
  }
  blog: {
    heading: string
    subtitle: string
    readMore: string
    minutes: string
  }
}
